## MODIFIED Requirements

### Requirement: Интерфейс трёх зацепок без совета покупать
Интерфейс SHALL после отчёта показать расход запуска: токены и стоимость либо «не найдено». README MUST не числить часы/90 дней/токены как несделанные дырки.

#### Scenario: Экран расхода
- GIVEN три точки собраны
- WHEN смотрят отчёт
- THEN есть строка про токены или пометка, что расход не найден
