# Tasks: add-project-bootstrap

Сначала красный тест, потом реализация. Сбора салонов нет.

## 1. Качество (ruff + mypy + pytest)

- [x] 1.1 Красный тест: одной команды качества ещё нет или она падает
- [x] 1.2 Реализация: одна команда гоняет ruff, mypy и pytest
- [x] 1.3 Проверка: команда завершается кодом 0 на каркасе
- [x] 1.4 GitHub Actions вызывает ту же команду

## 2. Секреты и gitignore

- [x] 2.1 Красный тест: `.env` не в git, `.env.example` обязателен
- [x] 2.2 Реализация: `.env.example` с `TWOGIS_API_KEY`, `YANDEX_MAPS_API_KEY`, `LLM_*`
- [x] 2.3 Красный тест: `*.db` / `*.sqlite` / `*.sqlite3` игнорируются
- [x] 2.4 Реализация: gitignore закрывает `.env` и файл SQLite

## 3. Тесты без сети

- [x] 3.1 Красный тест: прогон не ходит в интернет
- [x] 3.2 Реализация: внешние ответы только фикстурами
- [x] 3.3 Проверка: pytest зелёный без сети

## 4. Streamlit hello и Compose

- [x] 4.1 Красный тест: HTTP к приложению ещё недоступен
- [x] 4.2 Реализация: Streamlit hello, без зацепок и отчёта
- [x] 4.3 Красный тест: Compose без `LLM_*` не должен падать
- [x] 4.4 Реализация: Docker Compose поднимает hello без ключа модели
- [ ] 4.5 Проверка: после `docker compose up` Streamlit отвечает по HTTP (локально Docker нет — смотрим GitHub Actions)
